version: '3.8'

services:
  next:
    build:
      context: .
      dockerfile: .docker/Dockerfile
    image: next_prod
    container_name: next_prod
    command: node /app/server.js
    volumes:
      - .:/next-app
    ports:
      - "3000:3000"
